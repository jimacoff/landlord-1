<p><%= @account.name %> Account Dashboard</p>
<p>Users:</p>
<ul>
  <% @account.users.each do |user| %>
    <li>
      <% if @account.owner == user %>(Owner)<% end %>
      <%= user.first_name %> <%= user.last_name %>
    </li>
  <% end %>
</ul>